
#' Manual Gene Annotation Loader
#'
#' @export
generate_gene_annotation_manual <- function(genome = "hg38") {
  # Mock implementation for discussion/testing if needed
  # Real implementation would read from a provided GTF/GFF or cached object.
  # Since user said "try simpler query then full", and simpler worked but full failed.
  # The issue is likely size/timeout.
  NULL
}
