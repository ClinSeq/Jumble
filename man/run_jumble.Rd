% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{run_jumble}
\alias{run_jumble}
\title{Run Jumble Analysis}
\usage{
run_jumble(
  bam_file,
  reference_file,
  output_dir = ".",
  snp_vcf = NULL,
  somatic_vcf = NULL,
  cores = 1,
  genome = NULL,
  correction = "optim",
  hrd_model_file = NULL,
  exclude_long_fragments = FALSE,
  prefix = NULL,
  ...
)
}
\arguments{
\item{bam_file}{Path to the input BAM file.}

\item{reference_file}{Path to the reference RDS file.}

\item{output_dir}{Directory to save results.}

\item{snp_vcf}{Optional path to a VCF file with SNPs.}

\item{somatic_vcf}{Optional path to a VCF file with somatic variants.}

\item{cores}{Number of cores (currently unused).}

\item{genome}{Genome version.}

\item{correction}{String indicating the method: "optim" (L1+TV, default) or "rlm" (Robust LM).}

\item{hrd_model_file}{Optional path to a custom HRD model object (e.g. randomForest, glm, or function) saved as an RDS file. When supplied, a supplementary Custom HRD score is added to GIS output.}

\item{exclude_long_fragments}{If TRUE, use count_medium (fragments ≤300bp)
instead of count (all fragments) as the main depth signal. Set to TRUE when
using clipoverlap BAMs where the midpoint calculation may be affected by
TLEN inflation from overlap clipping.}

\item{prefix}{Optional prefix for output filenames. When supplied, it replaces
the sample name derived from the input BAM/counts basename, so results are
written as \code{<prefix>.jumble.csv}, \code{<prefix>.png}, etc.}

\item{...}{Additional arguments.}
}
\value{
A list containing results (targets, segments, etc.).
}
\description{
Runs the complete Jumble analysis pipeline on a query sample. For interactive HTML genome reports, use the standalone \code{\link{frankenplot}} function on the output files.
}
